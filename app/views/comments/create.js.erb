<% if @comment.errors.any? %>
 // handle
<% else %>
  var commentsContainer = document.querySelector('.comments');
  commentsContainer.insertAdjacentHTML('beforeend', '<%= j render "comment", comment: @comment, user_is_comments_author: true  %>');
  var input = document.querySelector('#comment_content');
  input.value = '';
  input.blur();
  var comments = document.querySelectorAll('.comment')
  var lastComment = comments[comments.length - 1]
  lastComment.scrollIntoView();
<% end %>
